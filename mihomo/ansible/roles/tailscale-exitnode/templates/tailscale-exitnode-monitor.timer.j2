[Unit]
Description=Run tailscale exit node health check + auto-fix every {{ tailscale_exitnode_monitor_interval }}s

[Timer]
OnUnitActiveSec={{ tailscale_exitnode_monitor_interval }}
OnBootSec=10
Persistent=false

[Install]
WantedBy=timers.target
