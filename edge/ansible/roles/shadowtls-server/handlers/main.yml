---
# Handlers for Shadow-TLS Server

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart shadow-tls-server
  ansible.builtin.systemd:
    name: shadow-tls-server
    state: restarted
