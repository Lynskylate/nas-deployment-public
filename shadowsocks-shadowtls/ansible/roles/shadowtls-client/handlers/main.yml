---
# Handlers for Shadow-TLS Client

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart shadow-tls-client
  ansible.builtin.systemd:
    name: shadow-tls-client
    state: restarted
