---
# Handlers for VictoriaTraces Service

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart victoriatraces
  ansible.builtin.systemd:
    name: victoriatraces
    state: restarted
