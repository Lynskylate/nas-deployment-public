server: "{{ k3s_agent_effective_server_url }}"
token: "{{ k3s_cluster_token }}"
node-ip: "{{ k3s_agent_effective_ip }}"
node-name: "{{ inventory_hostname }}"
{% if k3s_flannel_iface | default('') | length > 0 %}
flannel-iface: "{{ k3s_flannel_iface }}"
{% endif %}
