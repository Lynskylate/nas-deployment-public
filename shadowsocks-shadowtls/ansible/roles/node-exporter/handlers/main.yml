---
# NOTE: restart is also handled inline in the enable/start shell task.
# These handlers fire only when service file or binary content changes.
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted
