---
# Handlers for Envoy SNI Configuration

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart envoy
  ansible.builtin.systemd:
    name: envoy
    state: restarted
